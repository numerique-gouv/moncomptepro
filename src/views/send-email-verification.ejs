<section>
    <div class="container">
        <h3 class="section__title">Activation de votre compte</h3>

        <% if (locals.notifications) { %>
            <% notifications.forEach(({type, message}) => { %>
                <div class="notification <%= type %>">
                    <%= message %>
                </div>
            <% }) %>
        <% } %>

        <% if (displaySendEmailButton) { %>
        <form autocomplete="off" action="/users/send-email-verification" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">

            <div class="form__group">
                <input type="submit" value="Me renvoyer un email d'activation" class="button large">
            </div>
        </form>
        <% } %>

        <% if (locals.continueLink) { %>
            <a href="<%= continueLink %>" class="button large">Continuer</a>
        <% } %>
    </div>
</section>

<div class="container">
    <div class="text-center">
        <div class="container">
            <div class="text-center">
                <p><a href="https://api.gouv.fr">Retour à l'acceuil</a></p>
            </div>
        </div>
    </div>
</div>
