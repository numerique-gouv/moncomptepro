<div class="card-button-container">
    <button class="fr-btn btn--fullwidth" type="submit" <% if (locals.ariaLabel) { %>aria-label="<%= locals.ariaLabel; %>"<% } %>>
        <%= label; %>
    </button>
</div>
