<div>
  <h1 class="fr-h3">Rattachement en cours</h1>
  <div class="fr-card__desc">
    <p class="fr-mt-2w">
      <span aria-hidden="true">🕵️</span> Vous souhaitez rejoindre <%=
      organization_label; %> avec l’adresse email <%= email; %>.
    </p>
    <p>
      Un email professionnel (ex : votre-entreprise.com) a plus de chance d’être
      validé par notre équipe de modération.
    </p>
    <ul class="fr-btns-group fr-btns-group--sm fr-btns-group--inline-sm">
      <li>
        <a
          href="/users/start-sign-in"
          aria-label="Corriger l’email <%= email; %>"
          class="fr-btn fr-btn--secondary"
        >
          Corriger l’email
        </a>
      </li>
      <li>
        <form
          id="form-join-organization"
          action="/users/join-organization"
          method="post"
        >
          <input
            type="hidden"
            name="_csrf"
            value="<%= csrfToken %>"
            autocomplete="off"
          />
          <input
            type="hidden"
            name="siret"
            value="<%= siret %>"
            autocomplete="off"
          />
          <input
            type="hidden"
            name="confirmed"
            value="true"
            autocomplete="off"
          />

          <input
            aria-label="Continuer avec cet email (<%= email; %>)"
            class="fr-btn fr-btn--secondary"
            type="submit"
            value="Continuer avec cet email"
          />
        </form>
      </li>
    </ul>
  </div>
</div>
