<div class="box-container">
    <h3 class="section__title">Confirmation de votre email</h3>

    <form id="verify-email" action="/users/verify-email" method="post">
        <% if (locals.notifications) { %>
        <% notifications.forEach(({type, message})=> { %>
        <div class="notification multiline <%= type %>">
            <%= message %>
        </div>
        <% }) %>
        <% } %>

        <p>
            Merci de saisir ci-dessous le code de confirmation à 10 chiffres que nous vous avons envoyé
            à
            <b>
                <%= email %>
            </b>.
        </p>

        <input type="hidden" name="_csrf" value="<%= csrfToken %>" autocomplete="off" />


        <div class="form__group">
            <input class="form-control verify_email_token" required max="9999999999" min="0" type="number"
                name="verify_email_token" autofocus aria-label="Code" placeholder="Code">
        </div>

        <div class="form__group">
            <input type="submit" value="Confirmer mon email" class="button large">
        </div>
        <br>
        <br>
        <div class="container notification warning text-center">
            <form id="send-email-verification" autocomplete="off" action="/users/send-email-verification" method="post">
                <p>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" autocomplete="off">

                    Vous ne recevez pas le code ? <input type="submit" value="Recevoir un nouveau code"
                        class="link-submit">.
                </p>
            </form>
            <p>Toujours rien ? <a href="/users/help">Consultez notre page d'aide</a></p>
        </div>
    </form>
</div>

<div class="container text-center">
    <p>
        <a class="external-link" href="https://datapass.api.gouv.fr">
            Retour à l’accueil
        </a>
    </p>
</div>
